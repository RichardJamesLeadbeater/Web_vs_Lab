# todo does this have to be coded within script?
# UNFINISHED
def run_function_and_unpack_args_kwargs(fn, args, kwargs):
    # performs function with packed args and kwargs
    return fn(*args, **kwargs)